config:
  target: "http://localhost:4000"
  phases:
    - duration: 30
      arrivalRate: 500
scenarios:
  - flow:
      - get:
          url: "/api/products"
      - post:
          url: "/api/login"
          json:
            email: "ejemplo@example.com"
            password: "12345678"